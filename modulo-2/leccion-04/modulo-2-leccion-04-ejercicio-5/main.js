'use strict';

//const btnEl = getEl('.btn');
debugger;
function getEl (selector) {
  const btnEl = document.querySelector (selector)
  return btnEl;
}
const btnEl = getEl('.btn');
console.log (btnEl);

const img = getEl('.btn2');
console.log (img);